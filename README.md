# probe

Учебный сервис для лабораторной работы № 2 по дисциплине «Инструменты DevOps».

## Что умеет

| Метод и путь | Что отвечает |
|---|---|
| `GET /` | строка со словом-маркером и именем хоста |
| `GET /me` | JSON: ФИО, группа, слово-маркер, имя хоста, версия |

## Что заполнить перед сборкой

В файле `app.py` — строки `STUDENT`, `GROUP` и `MARKER_DEFAULT`.

## Переменные окружения

| Переменная | Что задаёт | По умолчанию |
|---|---|---|
| `MARKER` | слово-маркер в ответах | значение `MARKER_DEFAULT` |
| `APP_PORT` | порт внутри контейнера | `5000` |
| — | версия в ответе `/me` читается из файла `/app/VERSION` | `dev`, если файла нет |
